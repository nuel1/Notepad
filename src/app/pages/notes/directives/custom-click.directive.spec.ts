import { CustomClickDirective } from './custom-click.directive';

describe('CustomClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomClickDirective();
    expect(directive).toBeTruthy();
  });
});
